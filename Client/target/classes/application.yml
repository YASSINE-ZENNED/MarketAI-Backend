server:
  port: 8081
spring:
  application:
    name: Client
  datasource:
    password: root
    url: jdbc:postgresql://localhost:5432/MarketAIClient
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
